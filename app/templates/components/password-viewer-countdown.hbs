{{#if isBeingShown}}
  {{input value=passwordPlain}}
  <span>{{countdown}}</span>
{{else}}
  <button class="btn btn-primary btn-large btn-block" {{action "showPassword"}}>
    View password
  </button>
{{/if}}

import { modulo } from '../../../helpers/modulo';
import { module, test } from 'qunit';

module('Unit | Helper | modulo');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = modulo(42);
  assert.ok(result);
});

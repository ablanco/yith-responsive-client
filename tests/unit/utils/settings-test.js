import settings from '../../../utils/settings';
import { module, test } from 'qunit';

module('Unit | Utility | settings');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = settings();
  assert.ok(result);
});
